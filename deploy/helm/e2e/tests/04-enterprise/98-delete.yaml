apiVersion: kuttl.dev/v1beta1
kind: TestStep
metadata:
  name: uninstall-image-factory
commands:
  - command: helm uninstall image-factory --wait --timeout=120s
    namespaced: true
  - command: kubectl delete pvc e2e-grype-existing --ignore-not-found
    namespaced: true
