{{- if and .Values.auth.enabled (not .Values.auth.existingSecret) }}
{{- $htpasswd := .Values.auth.htpasswd | required ".Values.auth.htpasswd is required when auth.enabled=true and auth.existingSecret is empty." -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "image-factory.fullname" . }}-htpasswd
  namespace: {{ include "image-factory.namespace" . }}
  labels:
    {{- include "image-factory.labels" . | nindent 4 }}
type: Opaque
stringData:
  htpasswd: |
    {{- $htpasswd | nindent 4 }}
{{- end }}
